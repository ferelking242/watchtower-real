name: flutter_avif
version: 3.1.0
description: Web stub for flutter_avif — falls back to standard Image widget.
environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'
dependencies:
  flutter:
    sdk: flutter
