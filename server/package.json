{
  "name": "watchtower-server",
  "version": "0.1.0",
  "description": "Watchtower headless extension server — exposes JS extension evaluation as an HTTP API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "express": "^4.18.2",
    "express-rate-limit": "^7.2.0",
    "node-fetch": "^3.3.2",
    "xpath": "^0.0.34",
    "@xmldom/xmldom": "^0.9.5"
  }
}
